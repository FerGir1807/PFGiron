import { EstatusDirective } from './estatus.directive';

describe('EstatusDirective', () => {
  it('should create an instance', () => {
    //const directive = new EstatusDirective();
    //expect(directive).toBeTruthy();
  });
});
