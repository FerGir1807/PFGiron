import { NombreProfesorPipe } from './nombre-profesor.pipe';

describe('NombreProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
